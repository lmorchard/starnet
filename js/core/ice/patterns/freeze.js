// @ts-check
// freeze — stationary; never moves regardless of disturbance. Stub for session 1; wired in session 5.

export const freeze = {
  id: "freeze",
  onTick() {
    throw new Error("pattern 'freeze' not yet implemented — wired in session 5");
  },
};
