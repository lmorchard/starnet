// @ts-check
// sentry-radius — patrols within N hops of the host node. Stub for session 1; wired in session 8.

export const sentryRadius = {
  id: "sentry-radius",
  onTick() {
    throw new Error("pattern 'sentry-radius' not yet implemented — wired in session 8");
  },
};
