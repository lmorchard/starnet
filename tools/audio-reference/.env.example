# Copy to .env and fill in. Loaded automatically by `audio-reference` (cwd + parents).
# CLI flags --project / --location override these.
GOOGLE_CLOUD_PROJECT=your-gcp-project
GOOGLE_CLOUD_LOCATION=us-central1
