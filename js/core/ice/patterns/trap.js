// @ts-check
// trap — stationary pattern — fires only when triggered by player actions on the host. Stub for session 1; wired in session 2.

export const trap = {
  id: "trap",
  onTick() {
    throw new Error("pattern 'trap' not yet implemented — wired in session 2");
  },
};
